# ctpy examples

`make run` builds and runs all of them (uses ../include).

- `hello.cpp` — stdout captured at compile time
- `fib.cpp` — a Python result as a C++ array bound
- `config.cpp` — arg<> in, ctpy::file<> VFS, lift<> out into ctc
